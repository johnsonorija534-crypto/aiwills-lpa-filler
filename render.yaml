services:
  - type: web
    name: lpa-pdf-autofill
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GHL_API_KEY
        sync: false
